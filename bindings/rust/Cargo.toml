[package]
name = "dense-sim"
version = "0.2.0"
edition = "2021"
links = "dense_sim"
description = "Safe Rust wrapper over the libdense_sim C ABI"
build = "build.rs"

[lib]
name = "dense_sim"
path = "src/lib.rs"

[dependencies]
